---
import { getCollection } from 'astro:content';
import BaseHead from '../components/BaseHead.astro';
import '../styles/global.css';

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);

const featured = posts[0];
const rest = posts.slice(1, 4);

function formatDate(date: Date) {
  return date.toLocaleDateString('cs-CZ', { day: 'numeric', month: 'long', year: 'numeric' });
}
---

<!doctype html>
<html lang="cs">
  <head>
    <BaseHead title="Your Comfort Place" description="Blog Sabiny Černé." />
  </head>
  <body>
    <nav class="nav">
      <div class="logo">your comfort place</div>
      <div class="nav-links">
        <a href="/">vítej</a>
        <a href="/blog">prozkoumej</a>
        <a href="/o-mne">kdo jsem</a>
      </div>
    </nav>

    <section class="hero">
      <div class="hero-inner">
        <span class="hero-star" aria-hidden="true">✳</span>
        <h1 class="hero-h1">Zpomal<br>se mnou</h1>
        <p class="hero-desc">Místo pro myšlenky, pocity, wellness a pomalé tempo.</p>
        <p class="hero-name">Sabina Černá</p>
        <a href="/blog" class="hero-cta">Prozkoumej ✳</a>
      </div>
    </section>

    <main class="section">
      <div class="label">z poslední doby</div>
      <div class="articles">
        {posts.slice(0, 3).map((post, i) => (
          <a href={`/blog/${post.id}`} class={`article-row ${i % 2 === 1 ? 'reverse' : ''}`}>
            <div class="article-img">
              {post.data.heroImage
                ? <img src={post.data.heroImage} alt={post.data.title} style="width:100%;height:100%;object-fit:cover;" />
                : <span>{post.data.category ?? 'článek'}</span>
              }
            </div>
            <div class="article-body">
              <span class="tag">{post.data.category ?? 'Článek'}</span>
              <h2 class="article-title">{post.data.title}</h2>
              <p class="article-excerpt">{post.data.description}</p>
              <div class="article-meta">
                <span class="date">{formatDate(post.data.pubDate)}</span>
                <span class="read">Číst dál ✳</span>
              </div>
            </div>
          </a>
        ))}
      </div>
    </main>

    <footer class="footer">
      <span class="footer-text">© 2026 Your Comfort Place · Sabina Černá</span>
      <span class="footer-text">@cerna.sabi</span>
    </footer>
  </body>
</html>

<style>
  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 18px 32px;
    background: var(--sb);
  }
  .logo {
    font-family: var(--font-sans);
    font-size: 11px;
    letter-spacing: 0.08em;
    color: var(--sb-muted);
    font-weight: 400;
  }
  .nav-links {
    display: flex;
    gap: 28px;
  }
  .nav-links a {
    font-size: 11px;
    color: var(--sb-muted);
  }
  .nav-links a:hover { color: var(--sb-text); }

  .hero {
    padding: 64px 32px 56px;
    background: var(--sb);
  }
  .hero-inner {
    max-width: 520px;
  }
  .hero-star {
    font-size: 13px;
    color: var(--sb-muted);
    margin-bottom: 24px;
    display: block;
  }
  .hero-h1 {
    font-family: var(--font-serif);
    font-size: 64px;
    line-height: 1.05;
    color: var(--sb-text);
    font-weight: 300;
    margin: 0 0 24px;
  }
  .hero-desc {
    font-size: 13px;
    line-height: 1.7;
    color: var(--sb-muted);
    margin: 0 0 6px;
  }
  .hero-name {
    font-family: var(--font-serif);
    font-size: 15px;
    color: var(--sb-mid);
    font-style: italic;
    margin: 0 0 32px;
  }
  .hero-cta {
    font-size: 11px;
    letter-spacing: 0.12em;
    color: var(--sb-muted);
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 9px 20px;
    border-radius: 20px;
    border: 0.5px solid var(--sb-border);
    transition: border-color 0.15s, color 0.15s;
  }
  .hero-cta:hover { color: var(--sb-brown); border-color: var(--sb-mid); }

  .section { padding: 44px 32px; }

  .label {
    font-size: 10px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--sb-muted);
    margin-bottom: 28px;
  }

  .articles {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .article-row {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 0;
    border-top: 0.5px solid var(--sb-border);
    padding: 32px 0;
    align-items: center;
  }
  .article-row:last-child { border-bottom: 0.5px solid var(--sb-border); }
  .article-row.reverse { grid-template-columns: 2fr 1fr; }
  .article-row.reverse .article-img { order: 2; }
  .article-row.reverse .article-body { order: 1; padding-right: 40px; padding-left: 0; }

  .article-img {
    background: #C8B9A8;
    aspect-ratio: 3/4;
    max-width: 220px;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    border-radius: 2px;
  }
  .article-img span {
    font-size: 9px;
    letter-spacing: 0.08em;
    color: rgba(74, 32, 16, 0.3);
    text-transform: uppercase;
  }

  .article-body {
    padding-left: 40px;
  }

  .tag {
    font-size: 9px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--sb-brown);
    background: rgba(74, 32, 16, 0.08);
    padding: 4px 10px;
    border-radius: 2px;
    display: inline-block;
    margin-bottom: 14px;
  }

  .article-title {
    font-family: var(--font-serif);
    font-size: 28px;
    line-height: 1.25;
    color: var(--sb-text);
    font-weight: 300;
    margin: 0 0 12px;
    transition: color 0.15s;
  }
  .article-row:hover .article-title { color: var(--sb-mid); }

  .article-excerpt {
    font-size: 13px;
    line-height: 1.65;
    color: var(--sb-muted);
    margin: 0 0 20px;
  }

  .article-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .date { font-size: 10px; color: var(--sb-muted); }
  .read {
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--sb-brown);
  }

  .footer {
    padding: 20px 32px;
    display: flex;
    justify-content: space-between;
  }
  .footer-text { font-size: 11px; color: var(--sb-muted); }

  @media (max-width: 680px) {
    .nav { padding: 14px 16px; }
    .hero { padding: 40px 16px 36px; }
    .hero-h1 { font-size: 44px; }
    .section { padding: 28px 16px; }
    .article-row,
    .article-row.reverse { grid-template-columns: 1fr; gap: 20px; }
    .article-row.reverse .article-img { order: 0; }
    .article-row.reverse .article-body { order: 1; padding-right: 0; }
    .article-img { max-width: 100%; aspect-ratio: 4/3; }
    .article-body { padding-left: 0; }
    .article-title { font-size: 22px; }
    .footer { padding: 16px; flex-direction: column; gap: 8px; }
  }
</style>
